<%= render 'categorywell' %>

<%= @category.name %>
<%= Post.find(@category.post_ids).each do |post| %>
<%= post.title %>
<% end %>



<!--<%= Post.find(@category.post_ids).each do |post| %>
<%= post.title %>
<% end %> -->
<!-- <%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %> -->
